name: Hello Brijesh

on: pull_request
 push:
    branches:
      - main

jobs:
  my_job:
    name: Hello World
    runs-on: ubuntu-latest

    steps:
      - name: Hello World
        run: echo "Hello World"
